// Для примера с загрузкой страницы
export interface PageData {
  id: number;
  title: string;
  content: string;
}

// Для примера с диалогом
export interface DialogData {
  id: number;
  message: string;
  details: string;
}